package com.pinelabs.RnD.AndroidUI.Constants;

public class EnumsRepo {

    public enum propertyFileName {
        DEVICE
    }

    public enum deviceProperty {
        PLATFORM_NAME,
        PLATFORM_VERSION,
        DEVICE_NAME,
        UDID,
        NEW_COMMAND_TIMEOUT
    }
    public enum irisSettings{
        ROID,
        BPCLURL,
        PINEURL,
        VMSURL,
        ALPURL
    }
    public enum methodName{
        POST,
        GET,
        PUT,
        DELETE,
        PATCH
    }
}
